package api

import (
	"github.com/gin-gonic/gin"
)

func Follow(c *gin.Context) {

	/*followIDValue := c.Query("follow_id")

	followID, err := strconv.Atoi(followIDValue)
	if err != nil {
		c.JSON(http.)
	}*/
}
